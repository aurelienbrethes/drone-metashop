/* eslint-disable @typescript-eslint/no-explicit-any */
/* Wait for real products from woocommerce api to fix any types */

// TODO: remove dead code !

interface IGoodyColor {
    name: string;
    bgColor: string;
    selectedColor: string;
    src?: string;
    alt?: string;
}

interface IGoodySize {
    name: string;
}
